#include <stdio.h>

/**
 * main - prints the alphabets without q and e
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
        char C;

	C = 'a';
	while

	(C <= 'z') {
		if (C != 'q'  && C != 'z')
                            putchar(C);
	}
        putchar('\n');
        return (0);
}
